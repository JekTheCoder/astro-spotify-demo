---
import AppLayout from "./AppLayout.astro";
import Layout from "./Layout.astro";
import GeneralNav from "@/components/GeneralNav.astro";
import LibrariesNav from "@/components/LibrariesNav.astro";
import MediaPlayer from "@/components/MediaPlayer";
---

<Layout title="Stopify ;)">
  <AppLayout>
    <aside slot="aside" class="grid grid-rows-[auto_1fr] gap-2">
      <GeneralNav />
      <LibrariesNav />
    </aside>

    <slot slot="main" />

    <footer slot="player" class="">
      <MediaPlayer client:load transition:name="media-player" transition:persist />
    </footer>
  </AppLayout>
</Layout>
